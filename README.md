
### [Template Backend](https://github.com/dropways/deskapp)


| الدالة     | ماذا تفعل          |
| ---------- | ------------------ |
| `attach()` | يضيف فقط           |
| `detach()` | يحذف فقط           |
| `sync()`   | يحذف + يضيف + ينظف |


php artisan config:cache
php artisan route:clear